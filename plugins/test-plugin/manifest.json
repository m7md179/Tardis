{
  "name": "test-plugin",
  "version": "1.0.0",
  "displayName": "Test Plugin",
  "description": "A minimal plugin for testing the plugin system during development.",
  "summary": "Test plugin used during development to verify the plugin loader, permission guard, and plugin API work correctly.",
  "tier": 1,
  "main": "index.ts",
  "permissions": [
    "storage:read",
    "storage:write"
  ],
  "skills": [
    {
      "id": "test-plugin.ping",
      "description": "Returns a pong response. Used to verify the plugin is loaded and callable.",
      "aiInvocable": true,
      "actionType": "direct",
      "parameters": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "description": "Optional message to echo back."
          }
        }
      }
    }
  ]
}
