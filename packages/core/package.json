{
  "name": "@tardis/core",
  "version": "2.0.0",
  "description": "TARDIS v2 core — AI engine, plugin system, memory, events",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@tardis/db": "workspace:*",
    "@tardis/shared": "workspace:*",
    "cron-parser": "^5.5.0",
    "drizzle-orm": "^0.38.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "typescript": "^5.7.2"
  }
}
